# One setuptools command option that pyproject.toml has no place for.
# `egg_info` writes its metadata directory beside setup.py by default;
# this puts it under build/ with every other generated directory.
# The directory has to exist before the command runs, GNUmakefile creates it.
[egg_info]
egg_base = build
